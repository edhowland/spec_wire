#!/usr/bin/env bash

pidfile=ruby_server.pid

if [ -f ${pidfile} ] && ps -p $(cat ${pidfile}) >& /dev/null ; then
  # display note, if service running
  echo server already running
else
  # start service, if no service running
  ./ruby_server.rb >& sinatra.out &
  echo $! > ${pidfile}
fi

